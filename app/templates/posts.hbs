<h1>Stuff is working!</h1>

{{#each model as |post|}}
  <ul>
    <li>{{post.title}}</li>
    <li>{{post.content}}</li>
  </ul>
{{/each}}
{{#link-to "posts.new" class='btn btn-default btn-s'}}Create New Post{{/link-to}}
{{#link-to "application" class='btn btn-default btn-s'}}Back{{/link-to}}
{{outlet}}
